# Блок 8.1: Модель данных и запросы к БД для уведомлений модераторов

## Цель

Создать модель данных `ModeratorNotification` для хранения уведомлений модераторов о новых заявках и добавить функции для работы с этими уведомлениями в БД.

## Контекст

Необходимо хранить связь между модератором, заявкой и message_id уведомления, чтобы в дальнейшем можно было удалить уведомление после обработки заявки.

## Задачи

| № | Действие | Файл |
|---|----------|------|
| 1.1 | Добавить модель `ModeratorNotification` в `database/models.py` с полями: id, moderator_id, application_id, message_id, created_at. Добавить индексы для быстрого поиска. | `database/models.py` |
| 1.2 | Добавить импорт `Index` в `database/models.py` | `database/models.py` |
| 1.3 | Добавить функции в `database/queries.py`: `save_moderator_notification`, `get_moderator_notifications_for_application`, `delete_moderator_notification` | `database/queries.py` |
| 1.4 | Добавить импорт `ModeratorNotification` в `database/queries.py` | `database/queries.py` |

## Чек-лист

- [x] Модель `ModeratorNotification` создана с необходимыми полями и индексами
- [x] Функции для работы с уведомлениями добавлены в `database/queries.py`
- [x] Импорты обновлены

## Выполнено

✅ **Задача 1.1**: Добавлена модель `ModeratorNotification` в `database/models.py` с полями id, moderator_id, application_id, message_id, created_at и индексами для быстрого поиска.

✅ **Задача 1.2**: Добавлен импорт `Index` в `database/models.py`.

✅ **Задача 1.3**: Добавлены функции `save_moderator_notification`, `get_moderator_notifications_for_application`, `delete_moderator_notification` в `database/queries.py`.

✅ **Задача 1.4**: Добавлен импорт `ModeratorNotification` в `database/queries.py`.

# Блок 8.3: Сохранение уведомлений при отправке

## Цель

Изменить функцию `notify_moderators_new_application` для сохранения message_id уведомлений в БД при отправке.

## Контекст

При отправке уведомления модераторам о новой заявке нужно сохранить message_id каждого уведомления в БД, чтобы в дальнейшем можно было удалить эти уведомления.

## Задачи

| № | Действие | Файл |
|---|----------|------|
| 3.1 | В функции `notify_moderators_new_application` после отправки уведомления сохранять message_id в БД через `save_moderator_notification` | `handlers/user_handlers.py` |

## Чек-лист

- [x] После отправки уведомления сохраняется message_id в БД
- [x] Обработка ошибок при сохранении

## Выполнено

✅ **Задача 3.1**: Изменена функция `notify_moderators_new_application` в `handlers/user_handlers.py`. После отправки уведомления каждому модератору сохраняется message_id в БД через `save_moderator_notification`. Добавлена обработка ошибок при сохранении.

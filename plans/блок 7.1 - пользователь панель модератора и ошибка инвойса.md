# Блок 7.1: Пользователь — переход в панель модератора и ошибка инвойса

## Цель

Убрать отправку новых сообщений при переходе в панель модератора и при ошибке создания инвойса; всё показывать в одном главном сообщении пользователя.

## Контекст

- Сейчас при нажатии «Панель модератора» вызывается `callback.message.answer(...)` — в чате появляется новое сообщение.
- При ошибке в `create_stars_invoice` вызываются `callback.message.answer(...)` или `message.answer(...)` — снова новое сообщение.

Используем существующие: `update_user_main_message`, `get_moderator_panel_keyboard`, `get_back_to_menu_keyboard` из `utils.user_messages` и `keyboards.user_keyboards` / `keyboards.moderator_keyboards`.

## Задачи

| № | Действие | Файл |
|---|----------|------|
| 1.1 | В `callback_go_to_moderator_panel` убрать `callback.message.answer(...)`; вызывать `update_user_main_message(bot, user_id, "👮 Панель модератора", get_moderator_panel_keyboard())`. | `handlers/user_handlers.py` |
| 1.2 | В `create_stars_invoice` в блоках ошибки: вместо `callback.message.answer(...)` и `message.answer(...)` вызывать `update_user_main_message` с текстом ошибки и `get_back_to_menu_keyboard()`. | `handlers/user_handlers.py` |

## Чек-лист

- [x] `callback_go_to_moderator_panel`: переход в панель модератора через `update_user_main_message`.
- [x] `create_stars_invoice`: ошибка создания инвойса через `update_user_main_message`.

## Выполнено

✅ **Задача 1.1**: В `callback_go_to_moderator_panel` заменён `callback.message.answer(...)` на `update_user_main_message` с текстом "👮 Панель модератора" и клавиатурой панели модератора.

✅ **Задача 1.2**: В `create_stars_invoice` обработка ошибок заменена на `update_user_main_message` с текстом ошибки и клавиатурой "Назад в меню". Унифицирована обработка для обоих случаев (CallbackQuery и Message).

# Блок 8.2: Утилиты для удаления уведомлений

## Цель

Создать функцию для удаления всех уведомлений модераторов о заявке (удаление сообщений в Telegram и записей из БД).

## Контекст

После обработки заявки модератором (approve/reject) нужно удалить все уведомления о этой заявке у всех модераторов, так как заявка больше не в статусе "pending".

## Задачи

| № | Действие | Файл |
|---|----------|------|
| 2.1 | Добавить функцию `delete_moderator_notifications_for_application` в `utils/moderator_messages.py` для удаления сообщений в Telegram и записей из БД | `utils/moderator_messages.py` |

## Чек-лист

- [x] Функция `delete_moderator_notifications_for_application` создана
- [x] Функция удаляет сообщения в Telegram с обработкой ошибок
- [x] Функция удаляет записи из БД после удаления сообщений

## Выполнено

✅ **Задача 2.1**: Добавлена функция `delete_moderator_notifications_for_application` в `utils/moderator_messages.py`. Функция получает все уведомления для заявки, удаляет сообщения в Telegram с обработкой ошибок (включая случаи, когда сообщение уже удалено), и затем удаляет записи из БД.
